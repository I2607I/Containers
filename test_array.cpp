#include <iostream>
#include <array>

int main() {

    // std::array<int, 3> a{ {1, 2} };
    // size_t s = a.size();
    // for (auto i = 0; i < s; i++) {
    //     std::cout << a[i] << std::endl;
    // }
    int arr[5] {};
    for (auto i = 0; i < 5; i++) {
        std::cout << arr[i] << std::endl;
    }
    return 0;
}